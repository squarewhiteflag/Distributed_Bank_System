# 分布式银行系统项目解析与四人分工

本文档对课程项目说明进行了中文整理，并给出了适用于四人团队的分工方案，可作为项目规划与报告撰写基础。

## 1. 项目概要

本项目要求基于 **UDP 套接字** 在客户端-服务器架构下设计并实现一个 **分布式银行系统**。所有银行账户信息由服务器维护，客户端通过发送请求远程调用服务器服务，包括开户、销户、存款、取款、监控以及两个自定义操作。项目还需实现 **至少一次（at-least-once）** 与 **至多一次（at-most-once）** 的调用语义，同时模拟消息丢失并比较两种语义的结果差异。

### 项目核心要求

* 实现银行服务端程序（UDP）
* 实现客户端程序并与服务器通信（UDP）
* 手动设计消息格式，完成 **手工编解码（marshalling/unmarshalling）**
* **禁止使用** RMI、RPC、CORBA、Java序列化、输入输出流类
* 实现6个必需服务 + 2个自定义操作（1个幂等 + 1个非幂等）
* 支持多客户端并发监控（服务器维护监控客户端列表）
* 实现消息丢失模拟、重复消息检测、历史记录等机制
* 实现两种调用语义并通过实验对比
* 编写详细报告，包含设计、流程、实验分析等
* 演示系统并在多台机器运行客户端与服务器

---

## 2. 系统功能需求详解

### 2.1 账户数据模型

每个银行账户包含以下字段：
- **账户号**（Account Number）：整型，服务器自动分配
- **账户持有人姓名**（Name）：变长字符串
- **密码**（Password）：定长字符串
- **货币类型**（Currency Type）：枚举类型（如 USD, EUR, SGD 等）
- **账户余额**（Balance）：浮点数

### 2.2 必需服务（6个）

#### 服务1：开户（Open Account）
- **输入**：姓名、密码、货币类型、初始余额
- **输出**：新账户号
- **特性**：非幂等操作

#### 服务2：销户（Close Account）
- **输入**：姓名、账户号、密码
- **输出**：确认消息或错误信息
- **错误处理**：密码错误、账户不存在、账户不属于该用户
- **特性**：非幂等操作

#### 服务3：存款（Deposit）
- **输入**：姓名、账户号、密码、货币类型、金额
- **输出**：更新后的余额
- **错误处理**：密码错误、账户不存在
- **特性**：非幂等操作

#### 服务4：取款（Withdraw）
- **输入**：姓名、账户号、密码、货币类型、金额
- **输出**：更新后的余额
- **错误处理**：密码错误、余额不足、账户不存在
- **特性**：非幂等操作

#### 服务5：监控注册（Monitor Registration）
- **输入**：监控时长（秒数）
- **输出**：注册确认
- **行为**：
  - 服务器记录客户端IP地址和端口号
  - 在监控期间，任何账户更新（开户、销户、存款、取款等）都会触发回调
  - 服务器向所有已注册的监控客户端发送更新信息
  - 监控时长到期后自动移除客户端记录
- **客户端行为**：注册后阻塞等待回调消息，期间不接受新的用户输入
- **并发要求**：支持多个客户端同时监控

#### 服务6：回调通知（Callback Notification）
- **触发条件**：任何账户更新操作完成
- **发送内容**：被更新账户的完整信息（账户号、姓名、货币类型、余额等）
- **接收端**：所有已注册的监控客户端

### 2.3 自定义操作（2个）

**必须设计并实现以下两类操作：**

#### 操作A：幂等操作（任选其一实现）
推荐选项：
- **查询账户详情**：输入账户号和密码，返回账户完整信息
- **查询余额**：输入账户号和密码，仅返回当前余额
- **验证账户**：检查账户号和密码是否匹配

**幂等性说明**：重复执行不会改变系统状态

#### 操作B：非幂等操作（任选其一实现）
推荐选项：
- **转账**：从账户A转账到账户B，需验证双方密码
- **添加利息**：按比例增加账户余额
- **收取服务费**：扣除固定金额的服务费

**非幂等性说明**：重复执行会导致系统状态改变（如余额错误累加）

---

## 3. 技术实现要求

### 3.1 通信协议

- **传输协议**：仅使用 UDP（不允许使用 TCP）
- **服务器地址**：通过命令行参数传递给客户端
- **客户端地址**：服务器从接收到的请求中获取
- **消息格式**：完全自定义，必须手动实现序列化

### 3.2 消息编解码（Marshalling/Unmarshalling）

**必须手动实现以下数据类型的编解码：**

1. **整型**：使用 `htonl()` / `ntohl()` 转换字节序
2. **浮点型**：需自行设计编码方式（如转换为整型或字节数组）
3. **枚举类型**：转换为整型编码
4. **定长字符串**：直接按固定长度传输
5. **变长字符串**：添加长度前缀（如：4字节长度 + 实际字符串内容）

**禁止使用的工具：**
- Java序列化（Serializable）
- ObjectInputStream / ObjectOutputStream
- RMI、RPC、CORBA等现成框架

**推荐数据结构：**
- 使用字节数组（byte array）存储序列化后的消息

### 3.3 请求/回复消息设计

**每个消息应包含：**
- **请求ID**（Request ID）：用于检测重复请求
- **操作类型**（Operation Type）：标识执行哪个服务
- **参数字段**：根据操作类型动态变化
- **客户端标识**（可选）：用于区分不同客户端

**示例消息结构：**
```
[Request ID (4 bytes)] [Operation Type (4 bytes)] [Param1] [Param2] ...
```

### 3.4 调用语义实现

#### 至少一次（At-Least-Once）
- **客户端**：发送请求后设置超时，超时未收到回复则重传
- **服务器**：直接处理每个请求，不检测重复
- **问题**：非幂等操作可能被重复执行（如重复存款）

#### 至多一次（At-Most-Once）
- **客户端**：同样使用超时重传机制
- **服务器**：
  - 维护请求历史表（Request ID → 执行结果）
  - 收到请求时先检查是否为重复请求
  - 若是重复请求，直接返回历史结果，不重新执行
  - 若是新请求，执行后将结果存入历史表

**请求历史表设计要点：**
- 键：Request ID（或 Client ID + Request ID）
- 值：执行结果（用于快速返回）
- 清理策略：定期清理过期记录或使用LRU策略

### 3.5 消息丢失模拟

**实现要求：**
- 在发送端（客户端或服务器）随机丢弃消息
- 通过命令行参数控制丢包率（如 `--loss-rate=0.3` 表示30%丢包率）
- 用于测试超时重传机制和调用语义的正确性

**建议实现位置：**
- 客户端：在发送请求前以一定概率跳过发送
- 服务器：在发送回复前以一定概率跳过发送

### 3.6 并发处理说明

**服务器端：**
- **不需要**为每个请求创建新线程
- 假设请求在时间上间隔足够，服务器按顺序处理即可
- **需要**维护监控客户端列表，支持多个客户端同时监控

**客户端端：**
- **不需要**多线程
- 监控期间阻塞等待回调，不接受新的用户输入

---

## 4. 四人团队任务划分

下面的任务划分确保职责清晰、模块独立，可进行并行开发。

---

### **成员 A – 服务器核心逻辑与监控机制负责人**

**主要任务：**

1. **服务器框架搭建**
   - 实现服务器主循环（UDP接收-处理-回复）
   - 处理命令行参数（端口号、调用语义类型、丢包率等）

2. **账户数据管理**
   - 设计并实现账户数据结构
   - 维护账户列表（内存存储即可）
   - 实现账户号自动分配机制

3. **基础银行操作实现**
   - 开户（Open Account）
   - 销户（Close Account）
   - 存款（Deposit）
   - 取款（Withdraw）
   - 实现所有操作的参数验证和错误处理

4. **监控回调机制（服务器端）**
   - 维护监控客户端列表（IP、端口、过期时间）
   - 在账户更新时触发回调发送
   - 实现监控超时检查和自动清理

5. **自定义非幂等操作**
   - 设计并实现一个非幂等操作（如转账）
   - 确保该操作在至少一次语义下会出现问题

**输出成果：**
- `server.c` / `server.java` 主文件
- 账户管理模块代码
- 监控回调触发逻辑
- 服务器端设计说明文档

---

### **成员 B – 消息协议与编解码模块负责人**

**主要任务：**

1. **消息协议设计**
   - 设计所有请求/回复消息的统一格式
   - 定义操作类型编码（如：1=开户，2=销户...）
   - 设计请求ID生成规则
   - 设计变长参数的编码方式

2. **编解码工具实现**
   - 实现整型编解码（使用 `htonl` / `ntohl`）
   - 实现浮点型编解码
   - 实现枚举类型编解码
   - 实现变长字符串编解码（长度前缀方式）
   - 实现定长字符串编解码

3. **消息构造与解析接口**
   - 提供统一的消息序列化函数
   - 提供统一的消息反序列化函数
   - 确保跨平台兼容（如C++和Java混用时的数据表示）

4. **两个自定义操作的消息格式设计**
   - 为成员A的非幂等操作设计消息格式
   - 为成员D的幂等操作设计消息格式

**输出成果：**
- `protocol.h` / `protocol.java` 协议定义文件
- `marshalling.c` / `Marshalling.java` 编解码工具
- 消息格式详细说明文档（含示例字节序列）

---

### **成员 C – 调用语义与消息容错机制负责人**

**主要任务：**

1. **调用语义实现（客户端）**
   - 实现超时重传机制
   - 实现请求ID生成与管理
   - 区分至少一次和至多一次模式（通过命令行参数）

2. **调用语义实现（服务器端）**
   - 实现请求历史表（用于至多一次语义）
   - 实现重复请求检测
   - 实现历史结果快速返回
   - 实现历史表清理策略

3. **消息丢失模拟**
   - 实现客户端消息丢失模拟（随机丢弃发送的请求）
   - 实现服务器端消息丢失模拟（随机丢弃发送的回复）
   - 通过命令行参数控制丢包率

4. **实验设计与执行**
   - 设计对比实验：证明至少一次语义会导致非幂等操作错误
   - 设计对比实验：证明至多一次语义对所有操作都正确
   - 准备测试数据和测试脚本
   - 记录实验结果和日志

**输出成果：**
- 调用语义模块代码（客户端和服务器端）
- 消息丢失模拟开关实现
- 实验测试脚本
- 实验结果分析报告章节

---

### **成员 D – 客户端界面与监控接收负责人**

**主要任务：**

1. **客户端用户界面**
   - 实现命令行菜单系统
   - 接受用户输入（操作选择、参数输入）
   - 显示服务器返回的结果或错误信息
   - 实现用户退出功能

2. **客户端请求构造**
   - 调用成员B的编解码接口构造请求消息
   - 通过UDP发送请求到服务器
   - 接收并解析服务器回复

3. **监控注册与回调接收（客户端）**
   - 实现监控注册请求发送
   - 监控期间阻塞等待回调消息
   - 接收并显示服务器推送的账户更新信息
   - 监控超时后恢复正常交互

4. **自定义幂等操作**
   - 设计并实现一个幂等操作（如查询账户详情）
   - 在客户端界面添加该操作的调用入口

5. **命令行参数处理**
   - 解析服务器地址和端口号
   - 解析调用语义类型（至少一次或至多一次）

**输出成果：**
- `client.c` / `client.java` 主文件
- 客户端UI交互逻辑
- 监控接收循环代码
- 用户使用手册

---

## 5. 开发规范与注意事项

### 5.1 代码规范

- **注释要求**：所有源代码必须有详细注释
- **命名规范**：使用有意义的变量名和函数名
- **模块化**：各成员代码应尽量解耦，通过明确接口交互

### 5.2 接口约定

**成员B需提供的接口（示例）：**
```c
// 序列化请求消息
int marshal_request(byte* buffer, int request_id, int op_type, ...);

// 反序列化请求消息
int unmarshal_request(byte* buffer, int* request_id, int* op_type, ...);

// 序列化回复消息
int marshal_reply(byte* buffer, int request_id, int status, ...);

// 反序列化回复消息
int unmarshal_reply(byte* buffer, int* request_id, int* status, ...);
```

**成员C需提供的接口（示例）：**
```c
// 客户端：发送请求（带超时重传）
int send_request_with_retry(int socket, byte* request, int len);

// 服务器：检查是否为重复请求
bool is_duplicate_request(int request_id);

// 服务器：记录请求历史
void record_request_history(int request_id, byte* reply, int len);
```

### 5.3 测试要求

- 单元测试：各成员测试自己模块的基本功能
- 集成测试：全员协作测试完整流程
- 压力测试：测试多客户端监控场景
- 语义对比实验：成员C主导，全员参与

### 5.4 演示准备

**演示场景1：基本银行操作**
- 客户端1：开户、存款、取款、查询、销户

**演示场景2：监控回调**
- 客户端1：注册监控（60秒）
- 客户端2：执行开户、存款等操作
- 客户端1：实时接收并显示更新通知

**演示场景3：调用语义对比**
- 模拟消息丢失
- 展示至少一次语义的重复执行问题
- 展示至多一次语义的正确处理

---

## 6. 项目时间线（建议）

| 时间段 | 主要任务 | 负责人 |
|--------|---------|--------|
| **第1周** | 消息协议设计、服务器框架搭建 | 成员B、成员A |
| **第2周** | 客户端界面、基本银行操作实现 | 成员D、成员A |
| **第3周** | 调用语义实现、监控回调机制 | 成员C、成员A |
| **第4周** | 集成测试、实验执行、报告撰写 | 全员 |

---

## 7. 报告结构与分工

### 报告章节划分

**第1章：系统设计**（成员A + 成员B）
- 系统架构图
- 账户数据模型
- 服务描述
- 自定义操作设计说明

**第2章：消息协议设计**（成员B）
- 消息格式详细说明
- 编解码实现方法
- 示例消息字节序列

**第3章：调用语义实现**（成员C）
- 至少一次语义实现
- 至多一次语义实现
- 重复请求检测机制
- 请求历史管理

**第4章：监控回调机制**（成员A）
- 监控注册流程
- 客户端列表维护
- 回调触发逻辑

**第5章：实验设计与结果分析**（成员C）
- 实验场景设计
- 消息丢失模拟方法
- 语义对比实验结果
- 结果分析与讨论

**第6章：用户手册**（成员D）
- 编译与运行说明
- 命令行参数说明
- 操作示例

### 工作量分配表

| 成员   | 任务描述                           | 工作占比 |
| ------ | ---------------------------------- | -------- |
| 成员 A | 服务器核心逻辑、监控机制、非幂等操作 | 28%      |
| 成员 B | 消息协议设计与编解码实现            | 24%      |
| 成员 C | 调用语义、容错机制、实验设计        | 24%      |
| 成员 D | 客户端程序、监控接收、幂等操作      | 24%      |

---

## 8. 提交清单

### 8.1 源代码提交
- 所有源代码文件（.c / .java / .h 等）
- Makefile 或编译脚本
- README 文件（编译运行说明）
- 打包为一个压缩文件提交到 NTULearn

### 8.2 报告提交
- PDF 格式
- 包含封面（组员姓名、工作量分配）
- 包含完整的章节内容
- 单独提交到 NTULearn

### 8.3 演示准备
- 准备至少两台电脑
- 测试网络连通性
- 准备演示脚本和数据
- 时间：2026年2月7日（周六）

---

## 9. 重要提醒

⚠️ **严禁抄袭**：复制他人代码或将代码给他人复制将导致课程项目零分

⚠️ **截止日期**：2026年2月4日（周三）午夜前，不接受迟交

⚠️ **提交注意**：NTULearn 系统不允许重复提交，请确认最终版本后再提交

⚠️ **禁止使用的技术**：RMI、RPC、CORBA、Java 序列化、I/O Stream 类

✅ **必须使用**：UDP socket、手动编解码、字节数组

✅ **可选加分项**：客户端与服务器使用不同编程语言（如 C++ + Java）

---

## 附录：参考资料

- 课程讲义：Socket 编程、远程调用语义
- C语言网络函数：`htonl()`, `ntohl()`, `sendto()`, `recvfrom()`
- Java网络类：`DatagramSocket`, `DatagramPacket`
- 在线资源：NTULearn 课程网站的 Socket 编程笔记

---

**文档版本**：v2.0  
**最后更新**：2026年1月  
**维护人**：项目组全体成员